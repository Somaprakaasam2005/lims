# Database Configuration
# For SQLite (development):
DATABASE_URI=sqlite:///lims_dev.db

# For MySQL (production):
# DATABASE_URI=mysql+pymysql://username:password@localhost/lims_db

# For Postgres (e.g., Render free tier):
# DATABASE_URI=postgresql+psycopg2://USER:PASSWORD@HOST:PORT/DBNAME

# Secret key for Flask sessions (CHANGE THIS IN PRODUCTION!)
SECRET_KEY=your-secret-key-here-change-in-production

# Flask Environment
# Options: development, production
FLASK_ENV=development
FLASK_DEBUG=True
